export class Client {
    constructor(
                 public id ?: number,
                 public nameClient ?: string,
                 public codeIfuClient ?: string,
                 public phoneClient ?: string
    ) {}
 }
